{
  "name": "mun-fantasy",
  "private": true,
  "description": "Monorepo root — scripts forward to backend/frontend packages.",
  "scripts": {
    "ci": "npm run ci --prefix backend && npm run ci --prefix frontend",
    "db:seed": "cd backend && npm run db:seed",
    "db:seed:team": "cd backend && npm run db:seed:team --",
    "db:migrate": "cd backend && npx prisma migrate deploy",
    "prisma:generate": "cd backend && npm run prisma:generate",
    "prisma:migrate": "cd backend && npm run prisma:migrate",
    "prisma:deploy": "cd backend && npm run prisma:deploy",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
